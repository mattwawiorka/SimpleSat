## UI Requirements

Build the UI with WebView from the beginning.

We use WebView to leverage web development skills while building this plugin.

The first UI should include:

- One large `Saturation` knob
- A clear `Saturation` label
- Optional current value display
- Minimal styling suitable for an audio plugin

Do not add meters, presets, complex animations, or extra controls in version 1.
